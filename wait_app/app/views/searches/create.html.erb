<p>
  Birthday:
  <%= @bday %>
  <br>
  Matches:
  <% if @matches %>
    <%= @matches.length %>
    <ol>
      <% @matches.each do |match| %>
        <li>
          <% if match["position_number"] %>
            You are number <%= match["position_number"] %> in the waitlist
            <br>
          <% end %>
          <% match.each do |key, value| %>
            <%= key %>: <%= value %> <br />
          <% end %>
        </li>
      <% end %>
    </ol>
  <% end %>
</p>

<p>
  <%= link_to "New search", searches_path, class: "btn btn-md btn-info" %>
</p>
